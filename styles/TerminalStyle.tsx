import React from 'react';
import '../styles/terminal.css';

const TerminalStyle: React.FC = () => {
  return null; // This component only imports the terminal.css styles globally
};

export default TerminalStyle;
